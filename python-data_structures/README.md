Python - Data Structures: Lists, Tuples
#!/usr/bin/python3
def print_matrix_integer(matrix=[[]]):
    for row in matrix:
        for celda in (row):
            print("{:d}".format(celda), end=" ")
        print("")